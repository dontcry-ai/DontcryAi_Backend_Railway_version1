[phases.setup]
nixPkgs = ["python310"]
nixLibs = []

[phases.install]
dependsOn = ["setup"]
cmds = ["python3 -m pip install --upgrade pip", "python3 -m pip install -r requirements.txt"]

[start]
cmd = "gunicorn apps:app --bind 0.0.0.0:$PORT --workers 1 --timeout 300 --max-requests 1000"
